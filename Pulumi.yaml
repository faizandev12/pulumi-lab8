name: lab8test
description: Static website deployment
runtime:
  name: nodejs
  options:
    typescript: true
    packagemanager: npm
config:
  aws:region: us-east-1  # Only valid config entries
# Remove the pulumi:tags section completely
